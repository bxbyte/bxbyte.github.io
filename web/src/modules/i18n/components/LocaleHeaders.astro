---
import { getLocaleUrl } from "../content"
import { locales } from "../config"
---

{
	locales.filter((locale) => locale != Astro.currentLocale).map(
		(locale) => (
			<link
				rel="alternate" 
				hreflang={locale} 
				href={getLocaleUrl(Astro, locale)}
			>
		)
	)
}