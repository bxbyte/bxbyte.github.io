---
import type { HTMLAttributes, HTMLTag } from 'astro/types'

type Props<Tag extends HTMLTag> = {
	tag: Tag
} & HTMLAttributes<Tag>

const { tag: Tag, class: class_, ...props } = Astro.props
---

<Tag class:list={['block', class_]} {...props}>
	<slot />
</Tag>

<style lang="scss">
	@use '@/styles/layout.module.scss';

	.block {
		padding: layout.$window-padding-y layout.$window-padding-x;
		width: calc(100% - 2 * layout.$window-padding-x);
		max-width: layout.$window-max-width;
	}
</style>
